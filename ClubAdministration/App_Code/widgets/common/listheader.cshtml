@helper Show_ListHeader(string listheader)
{
  var header = "<div class='border rounded p-0 mb-3 mt-2 shadow-sm' style='font-size:0.8em'>" + 
        "<h2 class='bg-light shadow-sm border-bottom mb-3 rounded-top pt-3 pb-3 text-center'>" + @listheader + "</h2>" +
        "<div class='px-2'>" +
            "<table class='table text-center table-bordered table-hover w-100 table-responsive-sm'>";
    <p>
        @header
    </p>
}
