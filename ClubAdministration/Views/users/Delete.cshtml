@model ClubAdministration.Models.system.users

@{
    ViewBag.Title = "حذف" + " " + Model.fullName;
}

@widgets.common.ribbon.Show_Ribbon(Url, "users.delete.default", "آيا از حذف" + (" " + Model.fullName) + " مطمئن هستيد؟", rootParams: new { id = Model.ID })
@using (Html.BeginForm("Delete", "users", FormMethod.Post, new { @name = "usersform" }))
{
    @Html.AntiForgeryToken()
    <div class="mt-2 rounded-bottom shadow-sm mb-4">
        <h3 class="text-center rounded-top p-3 border bg-light m-0">اطلاعات پايه</h3>
        <div class="rounded-bottom px-2 py-2 border border-top-0 ">
            <table class="table table-striped table-responsive-sm">
                <tr>
                    <td>
                        كد شناسايي
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.ID)
                    </td>
                </tr>
                <tr>
                    <td>
                        كاربر
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.fullName)
                    </td>
                </tr>
                <tr>
                    <td>
                        كاربر
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.user_name)
                    </td>
                </tr>
            </table>
        </div>
    </div>
}