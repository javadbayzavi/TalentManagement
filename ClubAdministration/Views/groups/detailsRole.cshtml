@model ClubAdministration.Models.system.group_roles

@widgets.common.ribbon.Show_Ribbon(Url, "groups.roles.details", "جزئيات نقش " + (" " + Model.role.title), new { id = Model.ID }, new { id = Model.group.ID })

<div class="mt-2 rounded-bottom shadow-sm mb-4">
    <h3 class="text-center rounded-top p-3 border bg-light m-0">اطلاعات پايه</h3>
    <div class="rounded-bottom px-2 py-2 border border-top-0 ">
        <table class="table table-striped table-responsive-sm">
            <tr>
                <td>
                    كد شناسايي
                </td>
                <td>
                    @Html.DisplayFor(model => model.ID)
                </td>
            </tr>
            <tr>
                <td>
                    عنوان نقش
                </td>
                <td>
                    @Html.DisplayFor(model => model.role.title)
                </td>
            </tr>
        </table>
    </div>
</div>
